<UserControl x:Class="DataTransferApp.Net.Controls.AwesomeLabelTooltip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:control="clr-namespace:DataTransferApp.Net.Controls"
             x:Name="Root">

    <StackPanel Orientation="Horizontal" DataContext="{Binding ElementName=Root}">
        <fa:IconBlock Icon="{Binding Icon}" 
                      Foreground="{StaticResource InfoBrush}" 
                      FontSize="12" 
                      Margin="0,0,0,0" 
                      VerticalAlignment="Center">
            <fa:IconBlock.ToolTip>
                <ToolTip Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Left">
                    <control:AwesomeToolTip Title="{Binding TooltipTitle}" Description="{Binding TooltipDescription}" />
                </ToolTip>
            </fa:IconBlock.ToolTip>
        </fa:IconBlock>
        <TextBlock Text="{Binding LabelText}" Style="{StaticResource SubHeaderTextStyle}" VerticalAlignment="Center"/>
    </StackPanel>

</UserControl>
