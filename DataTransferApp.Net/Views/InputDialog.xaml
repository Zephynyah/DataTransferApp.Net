<Window x:Class="DataTransferApp.Net.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        Title="Input" 
        Height="180" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" x:Name="PromptText" Text="Enter value:" FontSize="13" Margin="0,0,0,10"/>
        <TextBox Grid.Row="1" x:Name="InputTextBox" FontSize="13" Padding="5" Margin="0,0,0,15"/>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Click="OkButton_Click" IsDefault="True" 
                    Background="{StaticResource SuccessBrush}" 
                    Foreground="{StaticResource BackgroundBrush}" 
                    Padding="20,8" 
                    Margin="0,0,10,0">
                <TextBlock Text="OK"/>
            </Button>
            <Button x:Name="CancelButton" Click="CancelButton_Click" IsCancel="True" 
                    Background="{StaticResource SecondaryBrush}" 
                    Foreground="{StaticResource BackgroundBrush}"
                    Margin="0,0,10,0" 
                    Padding="20,8">
                <TextBlock Text="Cancel"/>
            </Button>
        </StackPanel>
    </Grid>
</Window>
