<Window x:Class="DataTransferApp.Net.Views.ChangesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        xmlns:markdig="clr-namespace:Markdig.Wpf;assembly=Markdig.Wpf"
        xmlns:local="clr-namespace:DataTransferApp.Net.Views"
        mc:Ignorable="d"
        Title="What's New" Height="700" Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="{StaticResource BackgroundBrush}">

    <FrameworkElement.CommandBindings>
        <CommandBinding Command="{x:Static markdig:Commands.Hyperlink}" Executed="OpenHyperlink" />
    </FrameworkElement.CommandBindings>

    <Window.Resources>
        <Style TargetType="{x:Type markdig:MarkdownViewer}">
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with Logo and Title -->
        <Border Grid.Row="0" Background="{StaticResource DarkBrush}" Padding="15,10">    
            <StackPanel Orientation="Horizontal">
                <fa:IconBlock Icon="StackOverflow" Foreground="{StaticResource BackgroundBrush}" FontSize="32" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="What's New" FontSize="20" FontWeight="Bold" Foreground="{StaticResource BackgroundBrush}"/>
                    <TextBlock Text="Version History &amp; Updates" FontSize="12"  Foreground="{StaticResource LightBrush}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- The MarkdownViewer renders the bound string as a FlowDocument -->
        <markdig:MarkdownViewer  
            x:Name="MarkdownViewer"
            Grid.Row="1"
            Margin="10"
            Markdown="{Binding MarkdownContent}"  
            Pipeline="{Binding Pipeline}" 
            Foreground="#34495E"
            Background="Transparent"/> 
     

        <!-- Footer -->
        <StackPanel Grid.Row="2" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Close"
                    HorizontalAlignment="Center"
                    Padding="20,8"
                    Background="#3498DB"
                    Foreground="{StaticResource BackgroundBrush}"
                    BorderThickness="0"
                    FontSize="14"
                    Click="CloseButton_Click"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>