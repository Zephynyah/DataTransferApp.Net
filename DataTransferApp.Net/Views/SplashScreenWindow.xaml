<Window x:Class="DataTransferApp.Net.Views.SplashScreenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        Title="Data Transfer App"
        Height="400" Width="600"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource DarkBrush}"
        ResizeMode="NoResize"
        Topmost="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main Content -->
        <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Image Source="/app.png" Width="128" Height="128" Margin="0,0,0,20"/>
            <TextBlock Text="Data Transfer Application" FontSize="24" FontWeight="Bold" Foreground="{StaticResource BackgroundBrush}" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="Loading..." FontSize="16" Foreground="{StaticResource BackgroundBrush}" HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Progress Bar -->
        <ProgressBar Grid.Row="1" Height="4" Margin="20,0,20,20" IsIndeterminate="True" Foreground="{StaticResource PrimaryBrush}" Background="Transparent"/>
    </Grid>
</Window>