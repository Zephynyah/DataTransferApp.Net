<Window x:Class="DataTransferApp.Net.Views.FileViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        Title="File Viewer" 
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource DarkBrush}" Padding="15,10">
            <StackPanel>
                <TextBlock FontSize="20" FontWeight="Bold" Foreground="{StaticResource BackgroundBrush}">
                    <Run Text="📄 File Contents"/>
                </TextBlock>
                <TextBlock x:Name="FileNameText" FontSize="12" Foreground="{StaticResource LightBrush}" Margin="0,5,0,0"/>
                <TextBlock x:Name="FilePathText" FontSize="10" Foreground="{StaticResource LightBrush}" Margin="0,2,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- Content -->
        <Border Grid.Row="1" Background="White" Margin="10">
            <TextBox x:Name="FileContentTextBox"
                     IsReadOnly="True"
                     TextWrapping="NoWrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas"
                     FontSize="12"
                     Padding="10"/>
        </Border>
        
        <!-- Footer -->
        <Border Grid.Row="2" Background="{StaticResource LightBackgroundBrush}" Padding="15,10">
            <Button Click="CloseButton_Click" Style="{StaticResource ButtonStyle}" Background="{StaticResource SecondaryBrush}" HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <Path Data="{StaticResource CloseIconGeometry}" Fill="White" Width="14" Height="14" Stretch="Uniform" Margin="0,0,5,0"/>
                    <TextBlock Text="Close" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Border>
    </Grid>
</Window>
