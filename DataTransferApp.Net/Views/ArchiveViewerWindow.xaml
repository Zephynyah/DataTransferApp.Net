<Window x:Class="DataTransferApp.Net.Views.ArchiveViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        Title="Archive Contents" 
        Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource DarkBrush}" Padding="15,10">
            <StackPanel>
                <TextBlock FontSize="20" FontWeight="Bold" Foreground="{StaticResource BackgroundBrush}">
                    <Run Text="📦 Archive Contents"/>
                </TextBlock>
                <TextBlock x:Name="ArchiveNameText" FontSize="12" Foreground="{StaticResource LightBrush}" Margin="0,5,0,0"/>
                <TextBlock x:Name="ArchivePathText" FontSize="10" Foreground="{StaticResource LightBrush}" Margin="0,2,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- Content -->
        <Border Grid.Row="1" Background="White" Margin="10">
            <DataGrid x:Name="ArchiveDataGrid" RowHeight="28"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserSortColumns="True"
                      GridLinesVisibility="Horizontal"
                      AlternationCount="2"
                      AlternatingRowBackground="#F9F9F9">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="5*"/>
                    <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="5*"/>
                    <DataGridTextColumn Header="Size" Binding="{Binding SizeFormatted}" Width="1*"/>
                    <DataGridTextColumn Header="Compressed" Binding="{Binding CompressedSizeFormatted}" Width="1*"/>
                    <DataGridTextColumn Header="Modified" Binding="{Binding Modified}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
        
        <!-- Footer -->
        <Border Grid.Row="2" Background="{StaticResource LightBackgroundBrush}" Padding="15,10">
            <Grid>
                <TextBlock x:Name="StatusText" VerticalAlignment="Center"/>
                <Button Click="CloseButton_Click" Style="{StaticResource ButtonStyle}" Background="{StaticResource SecondaryBrush}" HorizontalAlignment="Right">
                    <StackPanel Orientation="Horizontal">
                         <fa:IconBlock Icon="Times" Foreground="{StaticResource BackgroundBrush}" FontSize="12" Margin="0,0,5,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Close" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>
